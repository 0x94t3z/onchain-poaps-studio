import type { Metadata } from 'next'; import './globals.css'; import { Providers } from './providers'; import { Header } from '@/components/header'; import { MiniAppReady } from '@/components/miniapp-ready'
export const metadata:Metadata={title:'Onchain POAPs — memories that live forever',description:'Create, distribute and collect fully onchain POAPs on Base.',openGraph:{title:'Onchain POAPs',description:'Memories that live forever — fully onchain on Base.',images:['/api/og']},other:{'fc:miniapp':JSON.stringify({version:'1',imageUrl:`${process.env.NEXT_PUBLIC_APP_URL||'http://localhost:3000'}/api/og`,button:{title:'Open Onchain POAPs',action:{type:'launch_miniapp',name:'Onchain POAPs',url:process.env.NEXT_PUBLIC_APP_URL||'http://localhost:3000',splashImageUrl:`${process.env.NEXT_PUBLIC_APP_URL||'http://localhost:3000'}/icon.svg`,splashBackgroundColor:'#eeff41'}}})}}
export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body><Providers><MiniAppReady/><Header/><main>{children}</main><footer><span>ONCHAIN POAPS · BASE SEPOLIA</span><span>Every pixel. Every memory. Forever.</span></footer></Providers></body></html>}
