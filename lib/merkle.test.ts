import {describe,expect,it} from 'vitest';import {getAddress} from 'viem';import {buildTree,normalizeAddresses} from './merkle'
describe('allowlist builder',()=>{it('normalizes and deduplicates addresses',()=>{const a='0x0000000000000000000000000000000000000001';expect(normalizeAddresses(`${a}\n${a}`)).toEqual([getAddress(a)])});it('creates a proof per address',()=>{const addresses=normalizeAddresses('0x0000000000000000000000000000000000000001\n0x0000000000000000000000000000000000000002\n0x0000000000000000000000000000000000000003');const tree=buildTree(addresses);expect(tree.root).toMatch(/^0x[0-9a-f]{64}$/);expect(tree.entries).toHaveLength(3);expect(tree.proofFor(addresses[0]).length).toBeGreaterThan(0)})})
